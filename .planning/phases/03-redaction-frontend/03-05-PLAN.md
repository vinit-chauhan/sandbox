---
phase: 03-redaction-frontend
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/RedactPage.tsx
  - frontend/src/__tests__/RedactPage.test.tsx
autonomous: true
requirements: [OUT-02, OUT-04]
gap_closure: true
must_haves:
  truths:
    - "For pasted text, download filename is output.clean.txt (not output.clean.clean.txt)"
    - "Redaction summary section expands on click with clear visual affordance"
  artifacts:
    - path: frontend/src/pages/RedactPage.tsx
      provides: "Download fallback output.txt; summary with chevron and distinct background"
  key_links:
    - from: frontend/src/pages/RedactPage.tsx
      to: downloadFilename
      via: "handleDownload uses fallback output.txt"
---
<objective>
Fix two RedactPage UX gaps: (1) Pasted text download produces output.clean.clean.txt — change fallback to output.txt so downloadFilename yields output.clean.txt. (2) Summary expand has weak visual affordance — add chevron, distinct background, aria attributes so users notice it expanded.
Purpose: Close UAT gaps 6 and 8.
Output: RedactPage.tsx with both fixes; tests updated
</objective>

<execution_context>
@/Users/vinit.chauhan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinit.chauhan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-redaction-frontend/03-UAT.md

Gap 1: "For pasted text, download filename should be 'output.clean.txt'"
Root cause: Fallback is "output.clean.txt"; downloadFilename() inserts ".clean" before extension → "output.clean.clean.txt".
Fix: Change fallback to "output.txt" so downloadFilename("output.txt") → "output.clean.txt".

Gap 2: "Redaction summary section expands on click to show PII counts by type"
Root cause: Expanded content has weak visual affordance — same bg-gray-50, thin border-t; users don't notice it expanded.
Fix: Add chevron that rotates on expand; use different background for expanded content; add aria-expanded and aria-controls.

Reference: .planning/debug/double-clean-suffix.md, .planning/debug/summary-no-expand.md
</context>

<interfaces>

From RedactPage.tsx (existing):
- downloadFilename(originalName: string): string — inserts ".clean" before extension
- handleDownload: a.download = downloadFilename(originalFilename ?? "output.clean.txt")
- Summary: button with onClick={() => setSummaryExpanded((v) => !v)}, conditional {summaryExpanded && <dl>...}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix download fallback for pasted text</name>
  <files>frontend/src/pages/RedactPage.tsx</files>
  <action>
In handleDownload, change the fallback from "output.clean.txt" to "output.txt":

```ts
a.download = downloadFilename(originalFilename ?? "output.txt");
```

downloadFilename("output.txt") returns "output.clean.txt" (inserts .clean before .txt). This fixes the double .clean suffix for pasted input.
  </action>
  <verify>
    <automated>grep "output.txt" frontend/src/pages/RedactPage.tsx</automated>
  </verify>
  <done>Pasted text download produces output.clean.txt</done>
</task>

<task type="auto">
  <name>Task 2: Improve summary expand visual affordance</name>
  <files>frontend/src/pages/RedactPage.tsx, frontend/src/__tests__/RedactPage.test.tsx</files>
  <action>
Update the expandable Redaction summary section (lines ~245-266):

1. **Chevron icon**: Add a chevron (▼ or similar) that rotates 180° when expanded. Use a simple SVG or Unicode character. Position it on the right side of the button. When summaryExpanded is true, rotate it (e.g. transform rotate-180 or equivalent).

2. **Distinct background**: Change the expanded content (the dl) from same bg-gray-50 to a slightly different background — e.g. bg-gray-100 or bg-white with a subtle border so it's visually distinct from the button row.

3. **Accessibility**: Add aria-expanded={summaryExpanded} and aria-controls="redaction-summary-content" to the button. Add id="redaction-summary-content" to the dl wrapper.

4. **Structure**: Keep the button as the trigger; the expanded content should be clearly a separate block below it. Ensure the chevron is inside the button for click target.

If no icon library is in use, use a simple inline SVG chevron or the Unicode character ▼ (U+25BC). Tailwind: transform and transition for smooth rotation.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/__tests__/RedactPage.test.tsx</automated>
  </verify>
  <done>Summary section has chevron, distinct background, aria attributes; users notice when expanded</done>
</task>

</tasks>

<verification>
- Pasted text → Download → filename is output.clean.txt
- Summary button shows chevron; clicking expands with visible content; chevron rotates
- RedactPage tests pass
</verification>

<success_criteria>
- UAT Test 6: Download with .clean suffix — pasted text gives output.clean.txt
- UAT Test 8: PII summary — expandable section expands on click with clear visual feedback
</success_criteria>

<output>
After completion, create `.planning/phases/03-redaction-frontend/03-05-SUMMARY.md`
</output>
