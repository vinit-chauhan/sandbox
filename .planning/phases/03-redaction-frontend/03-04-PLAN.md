---
phase: 03-redaction-frontend
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/FileUpload.tsx
  - backend/routers/documents.py
  - backend/services/file_parser.py
autonomous: true
requirements: [INP-01]
gap_closure: true
must_haves:
  truths:
    - "Chat file upload accepts .log, .json, .csv, .yaml, .txt (same as Clean Logs)"
  artifacts:
    - path: frontend/src/components/FileUpload.tsx
      provides: "ACCEPTED includes .log,.json,.csv,.yml,.yaml"
    - path: backend/routers/documents.py
      provides: "ALLOWED_EXTENSIONS includes .log,.json,.csv,.yml,.yaml"
    - path: backend/services/file_parser.py
      provides: "parse_file handles .log,.json,.csv,.yml,.yaml"
  key_links:
    - from: frontend/src/components/FileUpload.tsx
      to: backend/routers/documents.py
      via: "uploadFile API"
    - from: backend/routers/documents.py
      to: backend/services/file_parser.py
      via: "file_parser.parse_file"
---
<objective>
Extend Chat file upload to accept .log, .json, .csv, .yml, .yaml — matching Clean Logs. Currently FileUpload and backend only accept .pdf,.docx,.txt,.md.
Purpose: Close UAT gap — "Clean log allow csv,log,json,yaml,txt. But Chat does not."
Output: FileUpload.tsx, documents.py, file_parser.py updated
</objective>

<execution_context>
@/Users/vinit.chauhan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinit.chauhan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-redaction-frontend/03-UAT.md

Gap: "File upload supports text file types (.log, .txt, .json, .csv, .yaml, etc.) across both Clean Logs and Chat features"
Root cause: Chat FileUpload and backend documents.py/file_parser.py restrict to .pdf,.docx,.txt,.md only.
Reference: .planning/debug/chat-file-types.md

Clean Logs (RedactPage) already accepts: .log,.txt,.json,.csv,.yml,.yaml,.conf,.config,.ini,.xml,.md,.text
</context>

<interfaces>

From backend/services/file_parser.py:
```python
def parse_file(path: str, extension: str) -> str:
    # Returns plain text for RAG indexing
```

From backend/routers/documents.py:
```python
ALLOWED_EXTENSIONS = {".pdf", ".docx", ".txt", ".md"}
# ext = Path(file.filename or "").suffix.lower()
# if ext not in ALLOWED_EXTENSIONS: raise HTTPException(400, ...)
```

From frontend/src/components/FileUpload.tsx:
```typescript
const ACCEPTED = ".pdf,.docx,.txt,.md";
// <input accept={ACCEPTED} ... />
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend FileUpload and backend for text file types</name>
  <files>frontend/src/components/FileUpload.tsx, backend/routers/documents.py, backend/services/file_parser.py</files>
  <action>
1. **FileUpload.tsx**: Change ACCEPTED from ".pdf,.docx,.txt,.md" to include ".log,.json,.csv,.yml,.yaml". Keep .pdf and .docx. Result: ".pdf,.docx,.txt,.md,.log,.json,.csv,.yml,.yaml". Update the helper text below the drop zone from "PDF, DOCX, TXT, MD" to reflect the new types (e.g. "PDF, DOCX, TXT, MD, LOG, JSON, CSV, YAML").

2. **documents.py**: Add to ALLOWED_EXTENSIONS: ".log", ".json", ".csv", ".yml", ".yaml". Result: {".pdf", ".docx", ".txt", ".md", ".log", ".json", ".csv", ".yml", ".yaml"}.

3. **file_parser.py**: Extend parse_file to handle the new extensions. For .log, .json, .csv, .yml, .yaml — all are text files. Use Path(path).read_text(encoding="utf-8") for each. Add:
   - if ext in (".txt", ".md", ".log", ".json", ".csv", ".yml", ".yaml"): return Path(path).read_text(encoding="utf-8")
   Refactor existing .txt/.md handling into this block. Do not add external deps (e.g. PyYAML) — raw text is sufficient for RAG indexing.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run 2>/dev/null; cd ../backend && python -c "
from services.file_parser import parse_file
import tempfile
from pathlib import Path
for ext in ['.log', '.json', '.csv', '.yaml']:
    with tempfile.NamedTemporaryFile(suffix=ext, delete=False) as f:
        f.write(b'test content')
        f.flush()
        assert parse_file(f.name, ext) == 'test content'
        Path(f.name).unlink()
print('OK')
"</automated>
  </verify>
  <done>Chat accepts .log,.json,.csv,.yml,.yaml; backend parses them as text</done>
</task>

</tasks>

<verification>
- FileUpload ACCEPTED includes new types
- documents.py ALLOWED_EXTENSIONS includes new types
- file_parser.parse_file returns text for .log, .json, .csv, .yml, .yaml
- Upload a .log or .json file in Chat — succeeds and document is indexed
</verification>

<success_criteria>
- UAT Test 3: Upload a file — Chat supports same text file types as Clean Logs
</success_criteria>

<output>
After completion, create `.planning/phases/03-redaction-frontend/03-04-SUMMARY.md`
</output>
