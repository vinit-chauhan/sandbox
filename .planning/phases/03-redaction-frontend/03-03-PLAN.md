---
phase: 03-redaction-frontend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docker-compose.yml
autonomous: true
requirements: [INP-01, INP-02]
gap_closure: true
user_setup:
  - service: ollama
    why: "Backend in Docker cannot reach Ollama on host unless Ollama listens on 0.0.0.0"
    env_vars:
      - name: OLLAMA_HOST
        value: "0.0.0.0"
        source: "Set before starting Ollama: export OLLAMA_HOST=0.0.0.0"
    dashboard_config: []
must_haves:
  truths:
    - "User can run Ollama on host so backend in Docker reaches it"
  artifacts:
    - path: README.md
      provides: "Ollama Docker connectivity instructions"
    - path: docker-compose.yml
      provides: "Comment or reference to OLLAMA_HOST requirement"
  key_links:
    - from: backend
      to: "host.docker.internal:11434"
      via: "OLLAMA_BASE_URL env var"
      pattern: "OLLAMA_BASE_URL"
---

<objective>
Document OLLAMA_HOST requirement so backend in Docker can reach Ollama on host. Ollama binds to 127.0.0.1 by default; connections from host.docker.internal are rejected. User must run Ollama with OLLAMA_HOST=0.0.0.0.
Purpose: Close UAT gap — "Ollama unavailable; LLM detection skipped" when Ollama is running.
Output: README section + docker-compose comment; user knows to set OLLAMA_HOST
</objective>

<execution_context>
@/Users/vinit.chauhan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vinit.chauhan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-redaction-frontend/03-UAT.md

Gap: "LLM detection via Ollama works when Ollama is running with a model loaded"
Root cause: Ollama binds to 127.0.0.1; backend in Docker uses host.docker.internal; Ollama rejects connections from that interface.
Reference: .planning/debug/ollama-unavailable.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document OLLAMA_HOST in README</name>
  <files>README.md</files>
  <action>
Add a section "Ollama Docker Connectivity" (or integrate into Quick Start step 1) that explains:

1. **Problem**: When the backend runs in Docker, it connects to Ollama via host.docker.internal:11434. Ollama binds to 127.0.0.1 by default and rejects connections from the host's network interface.

2. **Fix**: Before starting Ollama, set OLLAMA_HOST=0.0.0.0 so Ollama listens on all interfaces:
   - macOS/Linux: `export OLLAMA_HOST=0.0.0.0` then `ollama serve` (or just `ollama run qwen3.5:4b` which starts serve)
   - Or run: `OLLAMA_HOST=0.0.0.0 ollama run qwen3.5:4b`

3. **Placement**: Add this as a prerequisite in the "Install & start Ollama" step (step 1 of Quick Start), or as a new subsection right after Architecture.

Keep existing content; only add the OLLAMA_HOST requirement. Do not remove or corrupt other README content.
  </action>
  <verify>
    <automated>grep -q "OLLAMA_HOST" README.md && grep -q "0.0.0.0" README.md</automated>
  </verify>
  <done>README documents OLLAMA_HOST=0.0.0.0 requirement for Docker users</done>
</task>

<task type="auto">
  <name>Task 2: Add docker-compose comment</name>
  <files>docker-compose.yml</files>
  <action>
Add a comment above the backend service (or near OLLAMA_BASE_URL) that references the OLLAMA_HOST requirement:

```yaml
# Backend reaches Ollama on host via host.docker.internal.
# Ollama must listen on 0.0.0.0: set OLLAMA_HOST=0.0.0.0 before starting Ollama.
```

Place it so it's visible when editing the compose file. Do not change any service definitions or env vars.
  </action>
  <verify>
    <automated>grep -q "OLLAMA_HOST" docker-compose.yml</automated>
  </verify>
  <done>docker-compose.yml includes comment about OLLAMA_HOST</done>
</task>

</tasks>

<verification>
- README contains OLLAMA_HOST=0.0.0.0 instructions
- docker-compose.yml has explanatory comment
- User who follows README can run Ollama so backend reaches it
</verification>

<success_criteria>
- UAT Test 2: Paste text and clean — LLM detection works when user sets OLLAMA_HOST before starting Ollama
</success_criteria>

<output>
After completion, create `.planning/phases/03-redaction-frontend/03-03-SUMMARY.md`
</output>
